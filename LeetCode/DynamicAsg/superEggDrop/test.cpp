#include <iostream>
#include <array>
int main()
{
    // constexpr int m = 2, n = 3;
    // // int dp[m][n];
    // // std::array<int, m> a;
    // // for (int i = 0; i < m; i++)
    // //     for (int j = 0; j < n; j++)
    // //         std::cout << dp[i][j] << std::endl;
    // std::array<std::array<int, n>, m> a;
    // for (auto i : a)
    //     for (auto j : i)
    //         std::cout << j << std::endl;
    int m = 2;
    int dp[m];
    dp[0] = 1;
}